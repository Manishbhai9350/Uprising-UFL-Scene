  #include <fog_pars_vertex>  uniform float pixelRatio;  uniform float time;  attribute float opacity;  attribute float speed;  attribute float size;  attribute float glow;  attribute vec3 noise;  attribute vec3 color;  varying vec3 vColor;  varying float vGlow;  varying float vOpacity;  void main() {    vec4 transform = vec4(position, 1.);    transform.x += cos(time * speed * noise.x) * 1.;    transform.y += sin(time * speed * noise.y) * 1.;    transform.z += cos(time * speed * noise.z) * 1.;    vec3 transformed = transform.xyz;    #include <project_vertex>    gl_PointSize = size * 25. * pixelRatio;    #include <fog_vertex>    vGlow = glow;    vColor = color;    vOpacity = opacity;  }