#include <fog_pars_fragment>  uniform sampler2D map;      varying vec3 vColor;  varying float vGlow;  varying float vOpacity;  void main() {    vec4 texel = vec4(texture2D(map, gl_PointCoord));    vec4 color = texel * vec4(vColor, 1.);    gl_FragColor = color + color * vGlow;    gl_FragColor.a *= vOpacity;    #include <tonemapping_fragment>    #include <encodings_fragment>    #include <fog_fragment>  }
